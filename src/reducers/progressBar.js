import * as actions from '../actions';

const progressBarReducer = (progressBar = { completed: 0 }, action) => {
    // switch (action.type) {
    //     case actions.RECALCULATE_PROGRESS:
    //         const newProgress = 10;
    //         console.log(action.categories);
    //
    //         return { completed: newProgress };
    //     default:
    //         return progressBar;
    // }
}

export default progressBarReducer;
